name: Daily Pipeline

# Superseded by daily-evaluation.yml — kept for historical reference only.
# All scheduled evaluation work now runs in daily-evaluation.yml.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Redirect notice
        run: echo "This workflow is superseded by daily-evaluation.yml"
